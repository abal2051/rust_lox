fun makeCounter() {
  var i = 0;
  fun count() {
    i = i + 1;
    print i;
  }

  return count;
}
var counter = makeCounter();
counter(); // "1".
counter(); // "2".

var new_counter = makeCounter();
new_counter();
new_counter();
new_counter();
counter();

